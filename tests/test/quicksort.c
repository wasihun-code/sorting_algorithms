#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int start, end, pivot, tmp, temp;
	int array[] = [7, 6, 10, 5, 9, 2, 1, 15, 7];

	pivot = array[0];
	start = array[0];
	end = array[8];

	do
	{
		start++;
	}while(array[j] > pivot);

	do
	{
		stop++;
	}while(array[j] <= pivot);
